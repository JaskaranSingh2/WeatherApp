import VanillaTilt from "https://cdn.skypack.dev/vanilla-tilt@1.7.0";

VanillaTilt.init(search, {
	max: 10,
	speed: 1000,
	perspective: 700,
});

VanillaTilt.init(display, {
	max: 10,
	speed: 1000,
	perspective: 700,
});
